# Session 3.3 - Logical Operators

JavaScript logical operators are used to combine or negate conditions to control the flow of a program, make decisions, and provide fallback values. In a previous session, the comparison operators evaluated the relationship between two values and always returned a boolean (`true` or `false`).
